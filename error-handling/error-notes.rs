// Makes Result like Option where you only need to provide the affirmative (Ok), the other type
// will always be the library's error

// Main function becomes error handling functions -> in module?
// Run function becomes the program itself
